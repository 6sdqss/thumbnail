# Copy file này thành `secrets.toml` và điền credentials riêng.
# TUYỆT ĐỐI KHÔNG commit secrets.toml lên GitHub (đã có trong .gitignore).
# Khi deploy Streamlit Cloud: vào Settings -> Secrets và dán nội dung tương tự.
#
# Nếu không cấu hình secrets, app sẽ dùng credentials mặc định:
#   username: ducpro
#   password: 234766

APP_USERNAME = "ducpro"
APP_PASSWORD = "234766"
